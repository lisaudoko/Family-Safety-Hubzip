// Supabase has been removed. This file is kept as an empty stub
// so any lingering import doesn't break the build during migration.
// It will be deleted once all imports are cleaned up.

export function getSupabase() {
  return null;
}
